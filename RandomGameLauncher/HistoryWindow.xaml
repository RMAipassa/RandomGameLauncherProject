<Window x:Class="RandomGameLauncher.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="History &amp; Stats"
        Width="860" Height="560"
        WindowStartupLocation="CenterOwner">
    <Grid x:Name="RootHost" Margin="12">
        <Border Background="{DynamicResource App.WindowBackgroundBrush}"
                BorderBrush="{DynamicResource App.BorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource App.CornerRadius.L}"
                Padding="12">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="History" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Button Content="Clear" Margin="14,0,0,0" Padding="10,6" Click="Clear_Click" />
                    <TextBlock Text=" " />
                </StackPanel>

                <TabControl Margin="0,12,0,0">
                    <TabItem Header="Launch History">
                        <DataGrid x:Name="HistoryGrid" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding TimeLocal}" Width="140" />
                                <DataGridTextColumn Header="Game" Binding="{Binding Name}" Width="*" />
                                <DataGridTextColumn Header="Platform" Binding="{Binding Platform}" Width="90" />
                                <DataGridTextColumn Header="OK" Binding="{Binding Launched}" Width="50" />
                                <DataGridTextColumn Header="Session (min)" Binding="{Binding SessionMinutes}" Width="110" />
                                <DataGridTextColumn Header="Tags" Binding="{Binding Tags}" Width="220" />
                                <DataGridTextColumn Header="Error" Binding="{Binding Error}" Width="220" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="Stats">
                        <ScrollViewer Padding="8">
                            <TextBlock Text="{Binding StatsText}" TextWrapping="Wrap" />
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </DockPanel>
        </Border>
    </Grid>
</Window>
